# frozen_string_literal: true

RSpec.describe FtcEvent do
  it 'has a version number' do
    expect(FtcEvent::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
